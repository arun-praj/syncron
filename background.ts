// Service worker stub for this design pass. It doesn't talk to the real
// Docker backend yet — no WebSocket connection, no message relaying. That
// wiring is deliberately deferred until the design layer is signed off.

export {}

console.log("[Syncron] background service worker ready (mock only)")
